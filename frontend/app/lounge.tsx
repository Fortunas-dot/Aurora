/**
 * Alias route for the pixel lounge — same screen as `pixel-room`.
 * Navigation to `/pixel-room` / `/lounge` is commented out in profile and settings until the feature is shown again.
 */
export { default } from './pixel-room';
